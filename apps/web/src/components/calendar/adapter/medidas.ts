/**
 * As medidas da grelha.
 *
 * Uma constante de desenho, não de domínio — por isso fica na app e não no
 * `packages/shared`, onde vivem as contas de fuso horário que as duas grelhas
 * partilham.
 */

/** Altura de um minuto, em píxeis. */
export const PX_POR_MINUTO = 1.4;
